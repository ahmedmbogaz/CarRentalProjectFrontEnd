import { FilterColourPipe } from './filter-colour.pipe';

describe('FilterColourPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColourPipe();
    expect(pipe).toBeTruthy();
  });
});
